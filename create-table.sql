create table word (
       id    bigserial,
       context text,
       vector text
);

create table bidword (
       id    bigserial,
       context text,
       vector text
);

create table query (
       id    bigserial,
       context text,
       vector text
);

